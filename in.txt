var my_array : array[3] integer;

routine bubble_sort(b : array[3] integer) is
    var start_loop : integer is 1
    var end_loop : integer is 3
    var start_second_loop : integer is 1
    var end_second_loop : integer is 3
    var flag : integer is 1
    var temp is 0
    var one is 1
    for j in start_loop..end_loop loop
        for i in start_second_loop..(end_second_loop-j) loop
            if b[i] > b[i + 1] then
                var temp is a[i]
                b[i] := b[i + 1]
                b[i + 1] := temp
                flag := 2
                end
            i := i + 1
            if flag = 1 then i := 3 end
        j := j + 1
        end
     end
end;

routine init_and_start(a: array[3] integer) is
    var e1 : integer is 1
    var e2 : integer is 2
    var e3 : integer is 3
    a[1] := e1
    a[2] := e2
    a[3] := e3
    bubble_sort(a)
    end;