#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>
typedef struct {
int i
;
} my_rec_type;
typedef my_arr my_arr[(3+1)];

my_rec_type my_rec = {0};
*int a(my_arr* rec) {
int* result = malloc ((3+1)*sizeof(int));
return result;
}
my_rec input;
int* in_arr;
int* result = a(in_arr);

int main (int argc, char *argv[]) {
in_arr = malloc ((3+1)*sizeof(int));
result = malloc (*sizeof(int));
    if (argc > 1) {
    }
}